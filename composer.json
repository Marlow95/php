{
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "require-dev": {
        "phpunit/phpunit": "^9",
        "codedungeon/phpunit-result-printer": "^0.32.0"
    },
    "scripts": {
        "start": "php -S localhost:5050",
        "test": "vendor/bin/phpunit",
        "testAll":  "vendor/bin/phpunit tests"
    },
    "scripts-descriptions": {
        "start": "Starts the server on PORT 5050",
        "test": "starts phpunit for unit testing {composer test tests/[name].php}",
        "testAll": "tests the entire tests folder"
    }
}
